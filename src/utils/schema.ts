/**
 * Schema utilities - re-exported from @musclemap/client for backwards compatibility
 *
 * @deprecated Import directly from '@musclemap/client' instead:
 *   import { Type, isValidationSchema, parseWithSchema } from '@musclemap/client';
 */
export {
  Type,
  isValidationSchema,
  parseWithSchema,
} from '@musclemap/client';
