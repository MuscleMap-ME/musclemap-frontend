/**
 * HTTP Client - re-exported from @musclemap/client for backwards compatibility
 *
 * @deprecated Import directly from '@musclemap/client' instead:
 *   import { request, clearRequestCache, apiHelpers } from '@musclemap/client';
 */
export {
  request,
  clearRequestCache,
  apiHelpers,
} from '@musclemap/client';
