/**
 * useWebSocket Hook - re-exported from @musclemap/client for backwards compatibility
 *
 * @deprecated Import directly from '@musclemap/client' instead:
 *   import { useWebSocket } from '@musclemap/client';
 */
export { useWebSocket } from '@musclemap/client';
export { useWebSocket as default } from '@musclemap/client';
